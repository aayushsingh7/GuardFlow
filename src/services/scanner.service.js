class ScannerService {
  async scanProjectDependencies() {
    // Scan's project dependencies for any vulnerability.
  }
  async scanOpenSourcePackages() {
    // scan's open source packages for any vulnerability.
  }
  async scanNetwork() {
    // scan of any open ports
  }
}

export default ScannerService;
