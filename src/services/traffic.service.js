class TrafficService {
  async addTraffic() {
    // add new traffic information into the db
  }
  async getTrafficInfo() {
    // fetches traffic information based on per minute, per hour and per day
  }
  async getRoutesTraffic() {
    // fetches all routes traffic
  }
  async getRouteTraffic() {
    // fetches a specific route traffic details
  }
}

export default TrafficService;
